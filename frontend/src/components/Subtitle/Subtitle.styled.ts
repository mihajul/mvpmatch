import styled from 'styled-components';

import { COLORS, FONT_SIZES, LINE_HEIGHTS } from '../../styles/variables';

export const Subtitle = styled.h2`
  font-style: normal;
  font-weight: 800;
  font-size: ${FONT_SIZES.SIZE_8};
  line-height: ${LINE_HEIGHTS.DEFAULT};
  color: ${COLORS.BRAND_1};
  margin: 0;
`;
