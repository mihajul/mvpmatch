import styled from 'styled-components';

import { FONT_SIZES } from '../../../styles/variables';

export const ToggleShowHideLink = styled.a`
  font-size: ${FONT_SIZES.SIZE_11};
  cursor: pointer;
`;
