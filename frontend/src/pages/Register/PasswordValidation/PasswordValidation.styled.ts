import styled from 'styled-components';

import { COLORS, FONT_SIZES, FONT_WEIGHTS } from '../../../styles/variables';

export const PasswordValidation = styled.div`
  margin-top: 20px;
  font-size: ${FONT_SIZES.SIZE_10};
`;

export const PasswordValidationTitle = styled.div`
  margin-bottom: 8px;
  color: ${COLORS.BRAND_1};
  font-size: ${FONT_SIZES.SIZE_10};
  font-weight: ${FONT_WEIGHTS.EXTRABOLD};
`;

export const PasswordValidationImage = styled.img`
  margin-right: 10px;
`;
export const PasswordValidationRule = styled.div`{
  display: flex;
`;
